<dc>decorator.describe</dc>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE html PUBLIC <dc>decorator.doctype</dc>>
<html <dc>decorator.xmlns</dc> xml:lang="en" lang="en">
<head>
  <title>Main Decorator test</title>
</head>
<body>

<include>main_test/header</include>

<if>company</if>
<p>If 1 ok.</p>
<endif/>

<if>company</if>
<p>If 2 ok.</p>
<else>
<p>If 2 not ok.</p>
<endif/>

<if>!company</if>
<p>If 3 ok.</p>
<endif/>

<if>!company</if>
<p>If 4 ok.</p>
<else>
<p>If 4 not ok.</p>
<endif/>

<if>enterprise</if>
<p>If 5 ok.</p>
<endif/>

<if>enterprise</if>
<p>If 6 ok.</p>
<else>
<p>If 6 not ok.</p>
<endif/>

<if>!enterprise</if>
<p>If 7 ok.</p>
<endif/>

<if>!enterprise</if>
<p>If 8 ok.</p>
<else>
<p>If 8 not ok.</p>
<endif/>


</body>
</html>