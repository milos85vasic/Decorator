<if>company</if>
    <if>company2</if>
        <p>If 1b ok.</p>
    <endif/>
<endif/>

<if>company</if>
    <if>!company2</if>
        <p>If 1b2 ok.</p>
    <endif/>
<endif/>

<if>!company</if>
    <if>company2</if>
        <p>If 1b3 ok.</p>
    <endif/>
<endif/>

---

<if>company</if>
    <if>company2</if>
        <p>If 2 ok.</p>
    <else>
        <p>If 2 not ok.</p>
    <endif/>
<else>
    <p>If 2 not not ok.</p>
<endif/>

<if>company</if>
    <p>If 2k ok.</p>
<else>
    <if>company2</if>
        <p>If 2k2 ok.</p>
    <else>
        <p>If 2k not ok.</p>
    <endif/>
<endif/>

<if>company</if>
    <if>company2</if>
        <p>If 2l ok.</p>
    <else>
        <p>If 2l not ok.</p>
    <endif/>
<else>
    <if>company2</if>
        <p>If 2l2 ok.</p>
    <else>
        <p>If 2l2 not ok.</p>
    <endif/>
<endif/>

---

<if>!company</if>
    <if>company2</if>
        <p>If 2b ok.</p>
    <else>
        <p>If 2b not ok.</p>
    <endif/>
<else>
    <p>If 2b2 not ok.</p>
<endif/>

<if>!company</if>
    <p>If 2c ok.</p>
<else>
    <if>company2</if>
        <p>If 2c2 ok.</p>
    <else>
        <p>If 2c not ok.</p>
    <endif/>
<endif/>

<if>!company</if>
    <if>company2</if>
        <p>If 2d ok.</p>
    <else>
        <p>If 2d not ok.</p>
    <endif/>
<else>
    <if>company2</if>
        <p>If 2d2 ok.</p>
    <else>
        <p>If 2d2 not ok.</p>
    <endif/>
<endif/>

---

<if>!company</if>
    <if>!company2</if>
        <p>If 2e ok.</p>
    <else>
        <p>If 2e not ok.</p>
    <endif/>
<else>
    <p>If 2e2 not ok.</p>
<endif/>

<if>!company</if>
    <p>If 2f ok.</p>
<else>
    <if>!company2</if>
        <p>If 2f2 ok.</p>
    <else>
        <p>If 2f2 not ok.</p>
    <endif/>
<endif/>

<if>!company</if>
    <if>!company2</if>
        <p>If 2g ok.</p>
    <else>
        <p>If 2g not ok.</p>
    <endif/>
<else>
    <if>!company2</if>
        <p>If 2g2 ok.</p>
    <else>
        <p>If 2g2 not ok.</p>
    <endif/>
<endif/>

---

<if>company</if>
    <if>!company2</if>
        <p>If 2h ok.</p>
    <else>
        <p>If 2h not ok.</p>
    <endif/>
<else>
    <p>If 2h2 not ok.</p>
<endif/>

<if>company</if>
    <p>If 2i ok.</p>
<else>
    <if>!company2</if>
        <p>If 2i2 ok.</p>
    <else>
        <p>If 2i not ok.</p>
    <endif/>
<endif/>

<if>company</if>
    <if>!company2</if>
        <p>If 2j ok.</p>
    <else>
        <p>If 2j not ok.</p>
    <endif/>
<else>
    <if>!company2</if>
        <p>If 2j2 ok.</p>
    <else>
        <p>If 2j2 not ok.</p>
    <endif/>
<endif/>

---

<if>!company</if>
    <if>company2</if>
        <p>If 3 ok.</p>
    <endif/>
<endif/>

<if>!company</if>
    <if>!company2</if>
        <p>If 3b ok.</p>
    <endif/>
<endif/>

---




<if>!company</if>
<p>If 4 ok.</p>
<else>
<p>If 4 not ok.</p>
<endif/>

<if>enterprise</if>
<p>If 5 ok.</p>
<endif/>

<if>enterprise</if>
<p>If 6 ok.</p>
<else>
<p>If 6 not ok.</p>
<endif/>

<if>!enterprise</if>
<p>If 7 ok.</p>
<endif/>

<if>!enterprise</if>
<p>If 8 ok.</p>
<else>
<p>If 8 not ok.</p>
<endif/>

// Multiple nesting 3 - 5 levels examples

