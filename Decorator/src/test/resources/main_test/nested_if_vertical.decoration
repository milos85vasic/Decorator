<if>company</if>
    <if>company2</if>
        <p>If 1b ok.</p>
    <endif/>
<endif/>

<if>company</if>
    <if>!company2</if>
        <p>If 1b2 ok.</p>
    <endif/>
<endif/>

<if>!company</if>
    <if>company2</if>
        <p>If 1b3 ok.</p>
    <endif/>
<endif/>

<if>company</if>
<p>If 2 ok.</p>
<else>
<p>If 2 not ok.</p>
<endif/>

<if>!company</if>
<p>If 3 ok.</p>
<endif/>

<if>!company</if>
<p>If 4 ok.</p>
<else>
<p>If 4 not ok.</p>
<endif/>

<if>enterprise</if>
<p>If 5 ok.</p>
<endif/>

<if>enterprise</if>
<p>If 6 ok.</p>
<else>
<p>If 6 not ok.</p>
<endif/>

<if>!enterprise</if>
<p>If 7 ok.</p>
<endif/>

<if>!enterprise</if>
<p>If 8 ok.</p>
<else>
<p>If 8 not ok.</p>
<endif/>

